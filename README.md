# ToDo-App
This is a simple Todo List application built with React and Vite. It features the ability to add, edit, delete, and mark todos as complete. The app uses React Context API for state management and stores data locally in the browser using local Storage. The user interface is styled with CSS for a modern, responsive look.
